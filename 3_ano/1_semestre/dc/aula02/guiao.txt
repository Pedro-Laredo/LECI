QUESTÃO 1: como as duas funções são chamadas como funções "normais" e a primeira entra logo num loop while true infinito, a task2 nunca começa a ser executada.

QUESTÃO 2 :
/*
 * SPDX-FileCopyrightText: 2010-2022 Espressif Systems (Shanghai) CO LTD
 *
 * SPDX-License-Identifier: CC0-1.0
 */

#include <stdio.h>
#include <inttypes.h>
#include "sdkconfig.h"
#include "freertos/FreeRTOS.h"
#include "freertos/task.h"
#include "esp_chip_info.h"
#include "esp_flash.h"
#include "esp_system.h"


static TaskHandle_t task1Handler = NULL;


void task1(void * params)
{
    while (true)
    {
        xTaskNotifyGive(task1Handler);
        printf("reading temperature \n");
        vTaskDelay(1000 / portTICK_PERIOD_MS);
    }
}

void task2(void * params)
{
    while (true)
    {
        printf("reading humidity \n");
        vTaskDelay(2000 / portTICK_PERIOD_MS);
        }
}
void app_main(void)
{
    xTaskCreate(task2, "Humidity", 2048, NULL, 2, &task1Handler);
    xTaskCreate(task1, "Temperature", 2048, NULL, 2, NULL);
    
}

QUESTAO 3:
/*
 * SPDX-FileCopyrightText: 2010-2022 Espressif Systems (Shanghai) CO LTD
 *
 * SPDX-License-Identifier: CC0-1.0
 */

#include <stdio.h>
#include <inttypes.h>
#include "sdkconfig.h"
#include "freertos/FreeRTOS.h"
#include "freertos/task.h"
#include "esp_chip_info.h"
#include "esp_flash.h"
#include "esp_system.h"


static TaskHandle_t task1Handler = NULL;


void task1(void * params)
{
    while (true)
    {
        xTaskNotifyGive(task1Handler);
        printf("reading temperature from %s\n", pcTaskGetName(NULL));
        vTaskDelay(1000 / portTICK_PERIOD_MS);
    }
}

void task2(void * params)
{
    while (true)
    {
        printf("reading humidity from %s\n", pcTaskGetName(NULL));
        vTaskDelay(2000 / portTICK_PERIOD_MS);
        }
}
void app_main(void)
{
    xTaskCreate(task2, "task1", 2048, NULL, 2, &task1Handler);
    xTaskCreate(task1, "task2", 2048, NULL, 2, NULL);
    
    
}

QUESTAO 4:
	sim, caso se remova o delay de uma task e aumentar a prioridade dessa mesma task, as tasks com prioridade mais baixa ficam bloqueadas uma vez que a que tem a prioridade mais alta está a correr em todos os instantes possíveis.
	

QUESTAO 5: 
	nenhuma? a esp32-c6 apenas possui um core então não é relevante qual escolher.

QUESTÃO 6:
	não há necessidade de colocar delay pois a tarefa fica automaticamente bloqueada se o valor da notifacação da tarefa for 0, quando o sender chama a funcão xTaskNotifyGive(), o valor da notificação é aumentado sendo a tarefa desbloqueada automaticamente.
	
	
